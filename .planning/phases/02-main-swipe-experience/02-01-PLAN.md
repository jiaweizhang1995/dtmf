---
phase: 02-main-swipe-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainRoute.kt
  - app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreen.kt
  - app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainUiState.kt
  - app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTokens.kt
  - app/src/androidTest/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTest.kt
autonomous: true
requirements: [UX-01]
user_setup: []
must_haves:
  truths:
    - "The main screen layout, hierarchy, and action placement visibly track the `main.jpg` reference instead of the placeholder Phase 1 screen"
    - "The Phase 2 screen structure exposes the top bar, thumbnail context row, metadata row, hero-photo area, bottom actions, banner row, and proceed affordance"
    - "Visual fidelity work does not prematurely implement Phase 3 undo/proceed behavior beyond placeholder affordance rendering"
  artifacts:
    - path: "app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreen.kt"
      provides: "Screenshot-faithful main-screen composition aligned to `main.jpg`"
    - path: "app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTokens.kt"
      provides: "Centralized spacing, color, and shape tokens for Phase 2 visual tuning"
    - path: "app/src/androidTest/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTest.kt"
      provides: "UI verification for visible sections and action affordances"
  key_links:
    - "MainRoute adapts the existing LaunchSession ready state into a dedicated Phase 2 main-screen model"
    - "MainScreen tokens keep screenshot-fidelity tuning localized instead of scattering literals across composables"
---

<objective>
Create the screenshot-faithful main-screen shell for Phase 2.

Purpose: Replace the Phase 1 placeholder with a stable Compose screen whose hierarchy, spacing, and action placement mirror `main.jpg`.
Output: A reusable main-screen composition, Phase 2 UI state model, and automated UI coverage for the required visual structure.
</objective>

<execution_context>
@/Users/jimmymacmini/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jimmymacmini/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-main-swipe-experience/02-RESEARCH.md
@.planning/phases/02-main-swipe-experience/02-VALIDATION.md
@main.jpg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 2 main-screen state and visual tokens</name>
  <files>app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainUiState.kt, app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTokens.kt, app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainRoute.kt</files>
  <action>Create a dedicated `MainUiState` for the swipe screen that derives display-ready values from the existing `LaunchSession` without introducing swipe-mutation logic yet. Add centralized spacing, color, typography, and shape tokens that let later plans tune the screen against `main.jpg` without scattering hardcoded values. Update `MainRoute` so it adapts ready-session data into this state model instead of rendering the placeholder text/button layout.</action>
  <verify>./gradlew testDebugUnitTest</verify>
  <done>The Phase 2 screen has an explicit state contract and localized design tokens ready for screenshot-fidelity composition.</done>
</task>

<task type="auto">
  <name>Task 2: Build the screenshot-faithful main-screen composition</name>
  <files>app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreen.kt, app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainRoute.kt</files>
  <action>Implement the full main-screen layout that mirrors `main.jpg`: top app row, thumbnail strip container, metadata row, hero-photo frame, bottom circular actions, low-emphasis banner row, and right-aligned `PROCEED` affordance. Keep the hero surface and controls visually present even if some interactions remain disabled or delegated to later plans. Use constraints and aspect-ratio-aware layout instead of hardcoded assumptions about one device size.</action>
  <verify>./gradlew assembleDebug</verify>
  <done>The app shows a stable Phase 2 main-screen shell whose hierarchy and action placement are visually aligned with the reference image.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for main-screen structure and affordances</name>
  <files>app/src/androidTest/java/com/jimmymacmini/wishdtmf/feature/main/MainScreenTest.kt</files>
  <action>Create Compose UI tests that render the ready-state main screen and assert the presence of the major screen sections and primary controls required by Phase 2. Use stable semantics/test tags for the top row, thumbnail rail, hero-photo container, bottom actions, and proceed affordance so later plans can extend the same tests without brittle selectors.</action>
  <verify>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=*.MainScreenTest</verify>
  <done>The screenshot-faithful main-screen structure is covered by executable UI checks instead of manual-only verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew assembleDebug` succeeds
- [ ] `./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=*.MainScreenTest` succeeds
- [ ] The placeholder Phase 1 main screen is fully replaced by a reference-aligned Phase 2 layout
</verification>

<success_criteria>

- All tasks completed
- The main screen visually matches the hierarchy and action placement of `main.jpg`
- Phase 2 introduces a dedicated main-screen state contract and visual tokens
- Automated UI coverage verifies the visible main-screen structure
</success_criteria>

<output>
After completion, create `.planning/phases/02-main-swipe-experience/02-01-SUMMARY.md`
</output>

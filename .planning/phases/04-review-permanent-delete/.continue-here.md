---
phase: 04-review-permanent-delete
task: 1
total_tasks: 3
status: ready_to_execute
last_updated: 2026-03-15T03:55:13.353Z
---

<current_state>
Phase 4 planning is complete and verified. The active work is paused before execution starts. The next concrete unit of work is 04-01, Task 1: replace the Phase 3 placeholder review route with a real staged-photo display model using a deterministic ordered staged-ID handoff plus review-boundary media resolution.
</current_state>

<completed_work>

- Phase 4 directory created at `.planning/phases/04-review-permanent-delete/`
- Research completed in `04-RESEARCH.md` with review UI, selection-state, `MediaStore` delete-flow, and validation architecture guidance
- Validation strategy created in `04-VALIDATION.md`
- Execute plans created:
  - 04-01: review shell, staged-photo rendering, screenshot fidelity
  - 04-02: review-local selection state and CTA/count behavior
  - 04-03: `MediaStore` confirmation flow and post-delete refresh contract
- Planning verification loop completed
- Two planning defects were fixed before pause:
  - 04-01 no longer assumes staged-photo order exists in the current `Set<Long>` handoff; it now requires an explicit deterministic ordered-ID companion list
  - 04-03 now routes post-delete refresh through `AppShell` and `LaunchViewModel` instead of pretending `AppNavGraph` owns that refresh seam
</completed_work>

<remaining_work>

- 04-01 Task 1: implement the deterministic ordered staged-ID handoff and review-boundary card resolution
- 04-01 Task 2: build `ReviewScreen` and `ReviewScreenTokens` to match `Delete-staging-area..jpg`
- 04-01 Task 3: add review instrumentation coverage and update nav tests
- 04-02: implement `ReviewViewModel`, `ReviewUiState`, selection toggling, count-driven copy, and SavedStateHandle restoration
- 04-03: implement delete-request coordination, Android confirmation launcher/result handling, and post-delete refresh through `AppShell`
- Update `.planning/STATE.md` once execution actually begins or a plan completes
</remaining_work>

<decisions_made>

- Preserve Phase 3 ownership boundaries: `MainViewModel` continues owning the broader swipe session, while review owns only review-local selection and delete-progress state
- Keep the navigation payload minimal; ordering metadata is allowed, but display-model resolution stays at the review boundary through the media layer
- Use dedicated review tokens for screenshot fidelity instead of scattering layout constants through composables
- After confirmed deletion, do not attempt to keep using the stale swipe batch; rebuild app state through a fresh launch-session refresh routed from `AppShell` into `AppNavGraph`
- Treat `04-VALIDATION.md` as intentionally not yet Nyquist-complete; Wave 0 test files and sign-off happen during execution, not planning
</decisions_made>

<blockers>
- No hard blocker for resuming execution
- Known risk: real-device verification is still needed for Android system delete confirmation behavior; emulator coverage alone is not enough
- Worktree is already dirty with unrelated planning/app changes (`.planning/STATE.md`, `.planning/config.json`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainScreen.kt`, screenshots, and `.idea/`), so execution should avoid reverting or overwriting those accidentally
</blockers>

<context>
The important mental model is that Phase 4 should deepen the existing review route without reopening Phase 3’s navigation decisions. Start by making the review entry contract structurally correct: ordered IDs plus media lookup. Once that boundary is stable, the UI fidelity work becomes straightforward, then review-local selection state can layer on cleanly, and only after that should delete confirmation and post-delete session refresh land. If anything feels ambiguous while resuming, trust the plan files over `.planning/STATE.md`; the state file still reflects the pre-Phase-4-planning position.
</context>

<next_action>
Start with: run `$gsd-execute-phase 4` and begin 04-01 Task 1 by reading `app/src/main/java/com/jimmymacmini/wishdtmf/app/navigation/AppNavGraph.kt`, `app/src/main/java/com/jimmymacmini/wishdtmf/feature/main/MainViewModel.kt`, `app/src/main/java/com/jimmymacmini/wishdtmf/feature/review/ReviewRoute.kt`, and the new Phase 4 plans to implement the ordered staged-ID handoff.
</next_action>

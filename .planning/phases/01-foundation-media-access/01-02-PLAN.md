---
phase: 01-foundation-media-access
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/src/main/java/.../data/media/MediaStorePhotoRepository.kt
  - app/src/main/java/.../data/media/PhotoQueryMapper.kt
  - app/src/main/java/.../feature/entry/PermissionCoordinator.kt
  - app/src/main/java/.../feature/entry/PermissionUi.kt
  - app/src/test/java/.../PhotoRepositoryTest.kt
  - app/src/test/java/.../PermissionCoordinatorTest.kt
autonomous: true
requirements: [MEDIA-01, MEDIA-02]
user_setup: []
must_haves:
  truths:
    - "The app can determine whether image permission is needed and request it through the modern Android permission flow"
    - "Eligible local photos can be queried from shared media through a repository implementation"
    - "The repository excludes unsupported media categories for Phase 1 and returns data suitable for random batch creation"
  artifacts:
    - path: "app/src/main/java/.../data/media/MediaStorePhotoRepository.kt"
      provides: "Real MediaStore-backed photo enumeration"
    - path: "app/src/main/java/.../feature/entry/PermissionCoordinator.kt"
      provides: "Version-aware permission logic for image access"
    - path: "app/src/test/java/.../PhotoRepositoryTest.kt"
      provides: "Automated coverage for repository query and filtering behavior"
  key_links:
    - "LaunchViewModel can use PermissionCoordinator and PhotoRepository without direct platform code in the UI"
    - "MediaStore query output maps into app-level LocalPhoto models for later session generation"
---

<objective>
Implement the real media-access layer for Phase 1.

Purpose: Make the app able to ask for permission correctly and enumerate eligible local photos from shared storage.
Output: Version-aware permission coordination, a MediaStore-backed photo repository, and tests for permission/query behavior.
</objective>

<execution_context>
@/Users/jimmymacmini/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jimmymacmini/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-media-access/01-CONTEXT.md
@.planning/phases/01-foundation-media-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement version-aware image permission coordination</name>
  <files>app/src/main/java/.../feature/entry/PermissionCoordinator.kt, app/src/main/java/.../feature/entry/PermissionUi.kt</files>
  <action>Create a permission coordinator that centralizes which runtime permission is needed by API level and how the entry flow transitions when permission is granted or denied. Use Activity Result APIs or a wrapper around them, not deprecated callback patterns. Keep the user-facing behavior minimal and direct, matching the context decisions.</action>
  <verify>./gradlew testDebugUnitTest --tests '*PermissionCoordinator*'</verify>
  <done>The app can evaluate permission state and drive the entry flow between request, denied, and granted outcomes without scattering permission logic across the UI tree.</done>
</task>

<task type="auto">
  <name>Task 2: Build the MediaStore-backed photo repository</name>
  <files>app/src/main/java/.../data/media/MediaStorePhotoRepository.kt, app/src/main/java/.../data/media/PhotoQueryMapper.kt</files>
  <action>Implement the repository contract using `ContentResolver` + `MediaStore` queries for image media only. Return app-level models containing stable IDs/URIs and the metadata needed for later phases. Keep the query focused on normal photo content and avoid assuming direct file-path access. Document any “best effort” exclusions for hidden/trash content instead of over-promising OEM-specific behavior.</action>
  <verify>./gradlew testDebugUnitTest --tests '*PhotoRepository*'</verify>
  <done>The repository can enumerate eligible local photos and return a list suitable for random batch selection.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for permission and query rules</name>
  <files>app/src/test/java/.../PhotoRepositoryTest.kt, app/src/test/java/.../PermissionCoordinatorTest.kt</files>
  <action>Create unit tests that verify API-level permission branching and the repository's mapping/filter behavior. Favor deterministic tests over brittle instrumentation here. Make sure tests cover “images only” and the fact that the result is allowed to contain fewer than 30 items.</action>
  <verify>./gradlew testDebugUnitTest</verify>
  <done>Permission branching and repository behavior are covered by repeatable automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew testDebugUnitTest` succeeds
- [ ] Permission flow is driven through one coordinator path
- [ ] Media repository returns app models rather than raw file-system paths
</verification>

<success_criteria>

- All tasks completed
- Permission model matches the targeted Android versions
- MediaStore query path is implemented behind a repository contract
- Unit tests cover permission and repository rules
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-media-access/01-02-SUMMARY.md`
</output>

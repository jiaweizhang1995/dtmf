---
phase: 01-foundation-media-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - build.gradle.kts
  - gradle.properties
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/.../MainActivity.kt
  - app/src/main/java/.../app/App.kt
  - app/src/main/java/.../app/navigation/AppNavGraph.kt
  - app/src/main/java/.../feature/entry/LaunchUiState.kt
  - app/src/main/java/.../feature/entry/LaunchViewModel.kt
  - app/src/main/java/.../data/media/PhotoRepository.kt
  - app/src/test/java/.../LaunchStateTest.kt
  - app/src/androidTest/java/.../EntryFlowTest.kt
autonomous: true
requirements: [MEDIA-01, UX-03]
user_setup: []
must_haves:
  truths:
    - "The project builds as a Compose-first Android app scaffold targeting the chosen SDK baseline"
    - "A lifecycle-safe launch state owner exists for permission, loading, ready, empty, and error states"
    - "Wave 0 test infrastructure exists for later plans to extend"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Android app module with Compose and test dependencies"
    - path: "app/src/main/java/.../feature/entry/LaunchViewModel.kt"
      provides: "Phase 1 launch/session state owner"
    - path: "app/src/test/java/.../LaunchStateTest.kt"
      provides: "Unit test harness for launch state transitions"
  key_links:
    - "MainActivity/App shell renders the entry flow using LaunchViewModel state"
    - "Test infrastructure covers launch-state code introduced in this plan"
---

<objective>
Create the Android project scaffold and application shell for Phase 1.

Purpose: Establish the baseline build, app structure, launch-state model, and test infrastructure that all later plans depend on.
Output: A compilable Compose Android app skeleton with a lifecycle-safe entry-state owner and initial tests.
</objective>

<execution_context>
@/Users/jimmymacmini/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jimmymacmini/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-media-access/01-CONTEXT.md
@.planning/phases/01-foundation-media-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Android Compose project and app shell</name>
  <files>settings.gradle.kts, build.gradle.kts, gradle.properties, app/build.gradle.kts, app/src/main/AndroidManifest.xml, app/src/main/java/.../MainActivity.kt, app/src/main/java/.../app/App.kt, app/src/main/java/.../app/navigation/AppNavGraph.kt</files>
  <action>Create the Android app project if it does not exist yet, using Kotlin, Compose, and the current stable Android SDK baseline from research. Keep the structure simple: one app module, one activity, Compose root, and a small navigation/app shell. Do not build the final screenshot-faithful UI yet; only create the minimal shell and placeholder routing needed for the Phase 1 entry flow.</action>
  <verify>./gradlew assembleDebug</verify>
  <done>The repository contains a compilable Android app skeleton with a Compose entry point and a clean feature/data package structure.</done>
</task>

<task type="auto">
  <name>Task 2: Establish launch-state ownership and domain contracts</name>
  <files>app/src/main/java/.../feature/entry/LaunchUiState.kt, app/src/main/java/.../feature/entry/LaunchViewModel.kt, app/src/main/java/.../data/media/PhotoRepository.kt</files>
  <action>Define the explicit launch-state model (`NeedsPermission`, `LoadingBatch`, `Ready`, `Empty`, `Error`) and create a `LaunchViewModel` that owns that state above the UI. Add the repository contract that later plans will implement for local image access. Keep the contract narrow and aligned with the context decisions about direct permission-driven entry and new-batch-on-relaunch behavior.</action>
  <verify>./gradlew testDebugUnitTest --tests '*LaunchState*'</verify>
  <done>The entry flow has a lifecycle-safe state owner and the data/domain seams needed for media and session implementation in later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 validation infrastructure</name>
  <files>app/build.gradle.kts, app/src/test/java/.../LaunchStateTest.kt, app/src/androidTest/java/.../EntryFlowTest.kt</files>
  <action>Install and configure the minimum unit and instrumented test setup needed by this phase. Create starter tests that exercise launch-state transitions and provide a place for later entry-flow UI assertions. Keep the test scaffolding real and runnable; do not leave placeholder files with no executable assertions.</action>
  <verify>./gradlew testDebugUnitTest</verify>
  <done>Phase 1 has working test infrastructure and at least one executable unit test around launch state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew assembleDebug` succeeds
- [ ] `./gradlew testDebugUnitTest` succeeds
- [ ] Launch state and test harness exist in the expected package structure
</verification>

<success_criteria>

- All tasks completed
- Android project scaffold is buildable
- Launch state owner exists and is not embedded only in composables
- Test infrastructure exists for later plans to extend
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-media-access/01-01-SUMMARY.md`
</output>

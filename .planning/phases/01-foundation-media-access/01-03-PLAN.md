---
phase: 01-foundation-media-access
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - app/src/main/java/.../domain/LaunchSessionBuilder.kt
  - app/src/main/java/.../feature/entry/LaunchViewModel.kt
  - app/src/main/java/.../feature/entry/EntryRoute.kt
  - app/src/main/java/.../feature/entry/EntryScreen.kt
  - app/src/main/java/.../feature/main/MainRoute.kt
  - app/src/test/java/.../LaunchSessionBuilderTest.kt
  - app/src/androidTest/java/.../EntryFlowTest.kt
autonomous: true
requirements: [MEDIA-02, UX-03]
user_setup: []
must_haves:
  truths:
    - "Granting permission immediately leads into a newly generated launch batch without a manual start step"
    - "The launch batch uses up to 30 eligible photos from the full library and proceeds with fewer when necessary"
    - "Rotation and normal background/foreground transitions preserve the active batch and current position"
  artifacts:
    - path: "app/src/main/java/.../domain/LaunchSessionBuilder.kt"
      provides: "Random batch creation logic for up to 30 photos"
    - path: "app/src/main/java/.../feature/entry/EntryScreen.kt"
      provides: "Minimal permission/loading/empty/error entry UI"
    - path: "app/src/androidTest/java/.../EntryFlowTest.kt"
      provides: "UI verification for entry-state continuity"
  key_links:
    - "LaunchViewModel uses MediaStorePhotoRepository results to build a batch through LaunchSessionBuilder"
    - "Entry UI renders launch states from LaunchViewModel and routes ready state into the future main flow"
---

<objective>
Complete the launch-session behavior and lifecycle continuity for Phase 1.

Purpose: Turn permission + repository foundations into the actual Phase 1 user-visible entry experience and batch creation behavior.
Output: Launch-session builder, minimal entry states, ready-state handoff, and lifecycle-preservation coverage.
</objective>

<execution_context>
@/Users/jimmymacmini/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jimmymacmini/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-media-access/01-CONTEXT.md
@.planning/phases/01-foundation-media-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement launch-session generation rules</name>
  <files>app/src/main/java/.../domain/LaunchSessionBuilder.kt, app/src/test/java/.../LaunchSessionBuilderTest.kt</files>
  <action>Create the domain logic that takes eligible local photos and produces a fresh random batch of up to 30 items on normal app launch. Make the rules explicit and tested: full-library candidate pool, ordinary photos only, and “use actual count if fewer than 30”. The logic should be deterministic under test through injected randomness or a controllable shuffler.</action>
  <verify>./gradlew testDebugUnitTest --tests '*LaunchSessionBuilder*'</verify>
  <done>Batch creation rules are explicit, testable, and aligned with the locked product behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the launch state machine into the entry UI and main-flow handoff</name>
  <files>app/src/main/java/.../feature/entry/LaunchViewModel.kt, app/src/main/java/.../feature/entry/EntryRoute.kt, app/src/main/java/.../feature/entry/EntryScreen.kt, app/src/main/java/.../feature/main/MainRoute.kt</files>
  <action>Connect permission results and repository output to the launch-state machine. Render minimal tool-like states for permission needed, loading, empty, denied, and error. When a batch is ready, transition directly into the main route placeholder without an extra start action. Preserve current batch metadata and index across configuration changes and normal background/foreground within the active process.</action>
  <verify>./gradlew assembleDebug && ./gradlew testDebugUnitTest</verify>
  <done>The app's entry flow matches the Phase 1 context decisions and immediately prepares the later main experience.</done>
</task>

<task type="auto">
  <name>Task 3: Add lifecycle and entry-flow UI verification</name>
  <files>app/src/androidTest/java/.../EntryFlowTest.kt</files>
  <action>Expand instrumented/Compose UI coverage to verify the minimal entry flow and state restoration behavior that Phase 1 promises. Include checks for permission-denied retry behavior, empty/error rendering, and configuration-change preservation of the current batch state where practical.</action>
  <verify>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=*.EntryFlowTest</verify>
  <done>Phase 1's visible launch behavior and continuity guarantees have executable UI verification coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew testDebugUnitTest` succeeds
- [ ] `./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=*.EntryFlowTest` succeeds
- [ ] Launching after permission grant immediately creates a batch and enters the ready flow
</verification>

<success_criteria>

- All tasks completed
- Random up-to-30 batch generation is implemented and tested
- Entry UI covers permission/loading/empty/error/ready states
- Active batch survives configuration change and normal in-session app backgrounding
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-media-access/01-03-SUMMARY.md`
</output>
